const globalRoom = "bc_21m";
const appTitle = "Couleur";

// Popular room configuration
const popularRoomTimeWindowMinutes = 5;
const popularRoomMinMessages = 2;
const popularRoomMinUniqueUsers = 2;
const popularRoomMaxDisplay = 5;

// Proof of Work configuration
const defaultMinimumPowDifficulty = 0;

// How long sending waits for our own profile before dropping the "n" tag
const nTagLookupTimeout = Duration(milliseconds: 300);
